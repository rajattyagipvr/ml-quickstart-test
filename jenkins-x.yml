buildPack: ml-python-service
pipelineConfig:
  pipelines:
    release:  
      pipeline:
        stage: build
        name: testing
        step:
          sh: source /root/.bashrc && python3 -m pip install -r requirements.txt && pytest -v
